schema_version: training_contract_v2
component: phase_c_world_model_with_long_risk
history_length: 4
short_rollout_horizon: 10
candidate_top_m: 10
state:
  node_channels: 10
  edge_channels: 6
  demand_channels_four_station: 9
action:
  node_channels: 8
  global_channels: 6
  edge_channels_saved_not_model_input: 4
labels:
  node_channels: 6
  station_channels: 2
  system_channels: 7
long_risk:
  model_owned_output_head: true
  jointly_optimized_with_world_model: true
  separate_head_only_training_stage: false
  data_seeds:
    train: {start: 681, stop_inclusive: 687}
    validation: [688, 689]
    offline_test: [690]
    online_test: {start: 691, stop_inclusive: 700}
  simulator_continuation_horizon: 200
  terminal_window: 50
  continuation_policy: frozen_greedy
  head_outputs: [peak_q90, peak_q95, cvar_q90, terminal_q90, delta_group_q90, event_logit]
  quantile_combo: {peak_q95: 0.2, cvar_q90: 0.3, terminal_q90: 0.5}
  stage1_loss_weight: 0.0
  stage2_loss_weight: 1.0
training:
  stage1: short_horizon_dynamics_warmup
  stage2: joint_dynamics_ranking_and_long_risk
  optimizer: {weight_decay: 0.0001, random_seed: 20260819}
canonical_trainer: WorldModel.training.run_train_v6
